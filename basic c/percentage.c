#include<stdio.h>

int main(){
       float m1 = 20; // maths
       float m2 = 30;  // physics
       float m3 = 40;  // chem
       float m4 = 35;  // eng
 
        float p = (m1 + m2 + m3 + m4 )/1.6;
        printf("%f \n",p);

    return 0;
}